
  # Single Page Betta Fish Sale

  This is a code bundle for Single Page Betta Fish Sale. The original project is available at https://www.figma.com/design/9GdMkArlheUqdSYix3DtLd/Single-Page-Betta-Fish-Sale.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  